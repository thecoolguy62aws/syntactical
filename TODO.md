# TODO

## Feature Ideas

- [ ] Add support for class inheritance (i.e, `class ChildClass(ParentClass) {}`)
- [ ] Add support for list of imports after the word import (i.e., `import something, something_else` or `from something import subsomething, other_subsomething`)
- [ ] Add tuples that are surrounded by (parentheses)
